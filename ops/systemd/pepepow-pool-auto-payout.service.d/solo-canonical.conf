[Service]
# Keep the existing Pool payout runtime unchanged. After it completes, run the
# guarded SOLO payout against the canonical production SOLO runtime through a
# wrapper that loads the persistent SOLO RPC environment without contaminating
# the parent Pool payout invocation.
ExecStartPost=/usr/bin/bash /home/ubuntu/pool-pepepow/ops/scripts/solo-auto-payout-canonical.sh
