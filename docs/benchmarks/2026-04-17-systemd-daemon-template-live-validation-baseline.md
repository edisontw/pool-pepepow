# 2026-04-17 Systemd Daemon-Template Live Validation Baseline

## Scope

This artifact records one short live validation smoke on the current
systemd-managed daemon-template Stratum path.

It verifies live accepted-share continuity and status/counter alignment only.
It does not make this a full validating pool.

## Environment

- daemon: `PEPEPOWd v2.9.0.4-c1394e6`
- stratum service: `pepepow-pool-stratum.service`
- stratum mode: `daemon-template`
- endpoint: `stratum+tcp://192.9.160.179:39333`
- observed miner remote: `36.227.104.10:50238`

## Live Accepted Share Evidence

Observed from:

- `/home/ubuntu/pool-pepepow/.runtime/live-stratum/share-events.jsonl`
- `/home/ubuntu/pool-pepepow/.runtime/live-stratum/stratum.log`

Observed facts:

- fresh accepted shares were present at `2026-04-17T10:40:17Z`
- `jobSource = "daemon-template"`
- `jobStatus` continued across `current` and short `previous`
- `shareHashValidationMode = "hoohashv110-pepew-header80"`
- `shareHashValidationStatus = "share-hash-invalid"`
- `targetValidationStatus = "candidate-possible"`
- `accepted_shares_total = 85482`
- `active_miners = 1`

Representative accepted submit:

- `jobId = "job-0000000000000097"`
- `jobSource = "daemon-template"`
- `remoteAddress = "36.227.104.10:50238"`
- `timestamp = "2026-04-17T10:40:17Z"`

## Snapshot / API Alignment

Observed alignment between:

- `/home/ubuntu/pool-pepepow/.runtime/live-stratum/activity-snapshot.json`
- `http://127.0.0.1:8080/api/health`

Aligned values:

- `templateModeEffective = "daemon-template"`
- `templateDaemonRpcStatus = "reachable"`
- `templateFetchStatus = "ok"`
- `shareHashValidationMode = "hoohashv110-pepew-header80"`
- `submitAcceptedCount = 3086`
- `submitRejectedCount = 0`
- `submitShareHashValidationCounts.share-hash-invalid = 3086`
- `submitShareHashValidationCounts.share-hash-valid = 0`
- `submitTargetValidationCounts.candidate-possible = 3086`

## Known Limitations

- `share-hash-invalid` remains the observed live result
- local share-hash handling is still classification-only
- `placeholderPayout = true` remains present in daemon-template preimage context
- this baseline does not imply safe accept/reject gating
- this baseline does not imply `submitblock` safety

## Post-Change Diagnostic Note

- Post-change restart verification completed at `2026-04-17T11:03:45Z` with `templateModeEffective = "daemon-template"`, `templateDaemonRpcStatus = "reachable"`, and `shareHashValidationMode = "hoohashv110-pepew-header80"` still aligned between `activity-snapshot.json` and `/api/health`.
- Fresh live accepted shares were observed again from `2026-04-17T11:06:49Z` through `2026-04-17T11:08:35Z` on the restarted daemon-template path.
- In that live post-restart share-log sample, `500` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` and all `500` classified to `shareHashDiagnostic.reasonCode = "header80-mismatch"`.
- Snapshot/API alignment remained intact during the live sample; at `2026-04-17T11:08:34Z` both surfaces reported `submitAcceptedCount = 498`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 498`, and `submitTargetValidationCounts.candidate-possible = 498`.
- Targeted daemon-template ingress smoke after the change also observed forced preimage failures classify to `template-context-mismatch` and `ntime-normalization-mismatch`.
- Header80 refinement verification completed after restart at `2026-04-17T11:16:38Z`; fresh live accepted shares were observed from `2026-04-17T11:19:12Z` through `2026-04-17T11:19:42Z`.
- In that live post-restart refinement sample, `137` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` with `shareHashDiagnostic.reasonCode = "header80-mismatch"`, and all `137` further classified to `shareHashDiagnostic.refinedReasonCode = "upstream-context-mismatch"`.
- The refined live sample consistently reported `mismatchFieldNames = ["merkleRoot"]`, `firstMismatchOffset = 36`, `endianOnlyPatternSuspected = false`, and `merkleMismatchAppearsUpstream = true`.
- Snapshot/API alignment remained intact during the refined live sample; at `2026-04-17T11:19:41Z` both surfaces reported `submitAcceptedCount = 136`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 136`, and `submitTargetValidationCounts.candidate-possible = 136`.
- Merkle provenance refinement verification completed after restart at `2026-04-17T11:26:41Z`; fresh live accepted shares were observed from `2026-04-17T11:27:10Z` through `2026-04-17T11:27:38Z`.
- In that live post-restart merkle refinement sample, `147` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` with `shareHashDiagnostic.reasonCode = "header80-mismatch"` and `shareHashDiagnostic.refinedReasonCode = "upstream-context-mismatch"`, and all `147` further classified to `shareHashDiagnostic.refinedMerkleReasonCode = "merkle-assembly-mismatch"`.
- The live merkle refinement sample reported `mismatchFieldNames = ["merkleRoot"]`, `merkleRootMismatchSourceCandidates = ["merkle-assembly-mismatch", "unknown-upstream-merkle-mismatch"]`, and stable template/job identifiers including `templateAnchor = "6e895cb898743ff6dc614ba1"` and `jobId = "job-0000000000000004"` in a representative sample.
- Snapshot/API alignment remained intact during the merkle refinement sample; at `2026-04-17T11:27:37Z` both surfaces reported `submitAcceptedCount = 145`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 145`, and `submitTargetValidationCounts.candidate-possible = 145`.
- Merkle assembly stage refinement verification completed after restart at `2026-04-17T11:36:25Z`; fresh live accepted shares were observed from `2026-04-17T11:36:53Z` through `2026-04-17T11:37:24Z`.
- In that live post-restart merkle assembly sample, `136` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` with `shareHashDiagnostic.refinedMerkleReasonCode = "merkle-assembly-mismatch"`, and all `136` further classified to `shareHashDiagnostic.refinedMerkleAssemblyStage = "coinbase-hash-stage"`.
- The live merkle assembly sample reported `branchCount = 0`, `firstMerkleDivergenceIndex = null`, `branchOrderVariantResults.reverseBranchOrder = null`, `branchEndianVariantResults.sourceOrderSiblings = null`, and `assemblyMismatchSourceCandidates = ["coinbase-hash-mismatch", "unknown-merkle-assembly-stage"]`.
- Snapshot/API alignment remained intact during the merkle assembly stage sample; at `2026-04-17T11:37:23Z` both surfaces reported `submitAcceptedCount = 131`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 131`, and `submitTargetValidationCounts.candidate-possible = 131`.
- Coinbase assembly refinement verification completed after restart at `2026-04-17T11:43:40Z`; fresh live accepted shares were observed from `2026-04-17T11:44:09Z` through `2026-04-17T11:44:25Z`.
- In that live post-restart coinbase refinement sample, `108` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` with `shareHashDiagnostic.refinedMerkleAssemblyStage = "coinbase-hash-stage"`, and all `108` further classified to `shareHashDiagnostic.refinedCoinbaseReasonCode = "placeholder-payout-output-suspected"`.
- The live coinbase refinement sample reported `coinbaseMismatchFieldNames = ["outputLayout", "placeholderPayout"]`, `coinbaseMismatchSourceCandidates = ["placeholder-payout-output-suspected", "output-layout-mismatch", "unknown-coinbase-assembly-stage"]`, `outputLayoutSummary.coinbaseOutputsCount = 3`, and `outputLayoutSummary.placeholderPayout = true` in a representative sample.
- Snapshot/API alignment remained intact during the coinbase refinement sample; at `2026-04-17T11:44:25Z` both surfaces reported `submitAcceptedCount = 108`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 108`, and `submitTargetValidationCounts.candidate-possible = 108`.
- Placeholder payout output comparison verification completed after restart at `2026-04-17T13:56:00Z`; fresh live accepted shares resumed on the daemon-template path from `2026-04-17T13:56:15Z`, and a post-restart accepted-share sample through `2026-04-17T13:56:55Z` contained `133` accepted shares with `shareHashDiagnostic.refinedCoinbaseReasonCode = "placeholder-payout-output-suspected"`.
- In that live comparison sample, all `133` accepted shares tested `correctedPlaceholderPayoutVariantTested = true`, all `133` reported `placeholderPayoutVariantEffect = "mismatch-unchanged"`, all `133` reported `correctedOutputLayoutComparisonResult = "share-hash-invalid"`, and all `133` reported `wouldCorrectedOutputLayoutRemoveMismatch = false`.
- A representative live sample reported `outputLayoutVariantDeltaSummary.variantName = "drop-placeholder-payout-output"`, `baselineOutputsCount = 3`, `correctedOutputsCount = 2`, `placeholderMatchCount = 1`, and `removedOutputAmount = 503750000000`; the corrected comparison path changed the local coinbase hash, derived merkle root, and header80, but it did not remove the share-hash mismatch.
- Snapshot/API alignment remained intact during the placeholder comparison step; at `2026-04-17T13:56:54Z` and `2026-04-17T13:56:55Z`, `activity-snapshot.json` and `/api/health` remained aligned on `templateModeEffective = "daemon-template"`, `shareHashValidationMode = "hoohashv110-pepew-header80"`, `submitAcceptedCount = 131`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 131`, and `submitTargetValidationCounts.candidate-possible = 131`.
- Output-layout refinement verification completed after restart at `2026-04-17T14:11:31Z`; the service briefly surfaced `daemon-template-fallback-synthetic` during startup before the first post-restart template fetch, then returned to `templateModeEffective = "daemon-template"` with `templateDaemonRpcStatus = "reachable"` and `templateFetchStatus = "ok"` by `2026-04-17T14:11:46Z`.
- Fresh live accepted shares resumed after the post-restart miner reconnect, and a post-restart accepted-share sample from `2026-04-17T14:11:46Z` through `2026-04-17T14:12:05Z` contained `83` accepted shares with `shareHashDiagnostic.refinedCoinbaseReasonCode = "placeholder-payout-output-suspected"`.
- In that live output-layout sample, all `83` accepted shares further classified to `shareHashDiagnostic.refinedOutputLayoutReasonCode = "ambiguous-output-layout-mismatch"`.
- The same live sample reported `outputCountComparison.decrementOutputCountOnlyTargetMatch = false`, `outputOrderComparison.movePlaceholderToEndTargetMatch = false`, `nonPlaceholderOutputMismatchSuspected = false`, and `coinb2BoundarySuspected = false` for all `83` accepted shares; the tested variant set remained `["decrementOutputCountOnly", "dropNonPlaceholderOutput1", "dropNonPlaceholderOutput2", "dropPlaceholderOutput", "movePlaceholderToEnd"]`, and all of those variants remained `share-hash-invalid`.
- A representative live sample reported `firstOutputMismatchIndex = 0`, `outputMismatchFieldNames = ["outputCount", "outputOrder", "outputAmount", "scriptPubKeyLength", "scriptPubKeyBytes"]`, a baseline output list of one placeholder output plus two non-placeholder P2PKH outputs, and a corrected output list that preserved the two non-placeholder outputs byte-for-byte after removing the placeholder output.
- Snapshot/API alignment remained intact during the output-layout refinement step; at `2026-04-17T14:12:05Z`, `activity-snapshot.json` and `/api/health` remained aligned on `templateModeEffective = "daemon-template"`, `shareHashValidationMode = "hoohashv110-pepew-header80"`, `submitAcceptedCount = 82`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 82`, and `submitTargetValidationCounts.candidate-possible = 82`.
- Authoritative daemon-template reference capture verification completed after restart at `2026-04-17T14:28:11Z`; fresh live accepted shares resumed after the miner reconnected at `2026-04-17T14:28:26Z`, and a post-restart accepted-share sample through `2026-04-17T14:29:06Z` contained `134` accepted shares.
- In that live authoritative-reference sample, all `134` accepted shares remained `shareHashValidationStatus = "share-hash-invalid"` and still refined through `shareHashDiagnostic.refinedMerkleReasonCode = "merkle-assembly-mismatch"`, `shareHashDiagnostic.refinedCoinbaseReasonCode = "placeholder-payout-output-suspected"`, and `shareHashDiagnostic.refinedOutputLayoutReasonCode = "ambiguous-output-layout-mismatch"`.
- The same live sample reported `authoritativeReferenceCaptured = true`, `authoritativeCoinbaseAvailable = false`, `authoritativeOutputLayoutAvailable = true`, `authoritativeComparisonResult = "equal"`, and `localVsAuthoritativeOutputLayoutEqual = true` for all `134` accepted shares.
- No authoritative mismatch fields were observed in that sample: `authoritativeMismatchFieldNames = []`, `firstAuthoritativeMismatchIndex = null`, and `authoritativeMismatchSourceCandidates = []` for all `134` accepted shares.
- A representative live sample reported an authoritative output vector with three entries: a placeholder miner output plus normalized `masternode` and `foundation` outputs; the authoritative coinbase summary reported `coinb1HexLength = 92`, `coinb2HexLength = 174`, and `coinb2Digest = "46e2a242ad93f71db11a798c"`, and the local failed-share output layout matched that authoritative normalized reference exactly.
- Snapshot/API alignment remained intact during the authoritative-reference step; at `2026-04-17T14:29:05Z` and `2026-04-17T14:29:06Z`, `activity-snapshot.json` and `/api/health` remained aligned on `templateModeEffective = "daemon-template"`, `shareHashValidationMode = "hoohashv110-pepew-header80"`, `submitAcceptedCount = 133`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 133`, and `submitTargetValidationCounts.candidate-possible = 133`.
- Non-output coinbase segment comparison verification completed after restart at `2026-04-17T14:46:22Z`; fresh live accepted shares resumed immediately, and a post-restart accepted-share sample through `2026-04-17T14:46:54Z` contained `120` accepted shares with `shareHashValidationStatus = "share-hash-invalid"`.
- In that post-restart sample, all `120` accepted shares still refined through `shareHashDiagnostic.refinedMerkleReasonCode = "merkle-assembly-mismatch"`, `shareHashDiagnostic.refinedCoinbaseReasonCode = "placeholder-payout-output-suspected"`, and `shareHashDiagnostic.authoritativeComparisonResult = "equal"`.
- The newest segment-level fields were present on fresh shares once the new process was live; in the most recent `50` accepted shares from that sample, all `50` further classified to `shareHashDiagnostic.refinedCoinbaseSegmentReasonCode = "coinbase-to-merkle-handoff-ambiguity"`.
- The same `50`-share segment sample reported `matchingCoinbaseSegments = ["coinbasePrefixBytes", "coinbaseLengthVarint", "scriptSigTemplateBytes", "extranonceRegion", "postScriptSigSequence", "outputCountVarint", "coinbaseTail"]`, `mismatchingCoinbaseSegments = []`, `coinbaseSegmentMismatchFieldNames = []`, `outputVectorConfirmedEqual = true`, and `localVsAuthoritativeNonOutputSegmentsEqual = true`.
- This reduced the remaining ambiguity: authoritative normalized output layout and all authoritative non-output coinbase segments matched the local reconstruction for those fresh shares, so the remaining mismatch layer is best localized to the `coinbase-to-merkle-handoff-ambiguity` boundary rather than any specific coinbase segment.
- Snapshot/API alignment remained intact during the segment-comparison step; `/api/health` at `2026-04-17T14:46:50Z` reported `submitAcceptedCount = 50`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 50`, and `submitTargetValidationCounts.candidate-possible = 50`, and the matching `activity-snapshot.json` at `2026-04-17T14:47:02Z` reported the same counts with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Full-coinbase handoff probe verification completed after restart at `2026-04-17T15:04:34Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-17T15:05:16Z` contained `122` accepted shares with the new handoff fields present.
- In that fresh handoff sample, all `122` accepted shares reported `fullCoinbaseBytesEqual = true`, `firstFullCoinbaseMismatchOffset = null`, `coinbaseLeafHashEqual = true`, and `merkleLeafNormalizationEqual = true`.
- Representative fresh live shares reported identical authoritative and local leaf hashes (`15b90756b74e7981f31e12ba12f6b3000d7d70d3b165228e3c353d247658303f`) and identical single-leaf merkle inputs, with `authoritativeMerkleLeafInputSummary.computedMerkleRoot`, `localMerkleLeafInputSummary.computedMerkleRoot`, and the observed header merkle root all equal.
- The same sample therefore classified to `refinedHandoffReasonCode = "unknown-handoff-mismatch"` rather than a serialization, leaf-hash, or single-leaf handoff mismatch; this probe did not expose any remaining difference in the complete coinbase transaction bytes or immediate leaf handoff artifacts.
- Snapshot/API alignment remained intact during the handoff-probe step; `/api/health` at `2026-04-17T15:05:10Z` reported `submitAcceptedCount = 122`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 122`, and `submitTargetValidationCounts.candidate-possible = 122`, and the matching `activity-snapshot.json` at `2026-04-17T15:05:16Z` reported the same counts with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Final header80 field probe verification completed after restart at `2026-04-17T15:15:11Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-17T15:15:51Z` contained `75` accepted shares, with the final-header fields present on the most recent `71` entries after startup.
- In that fresh header80 sample, all `71` entries with the new fields reported `header80BytesEqual = true`, `firstHeader80MismatchOffset = null`, and `headerFieldEquality = {"version": true, "prevhash": true, "merkleRoot": true, "ntime": true, "bits": true, "nonce": true}`.
- The same `71`-share sample reported `prevhashByteOrderSuspected = false`, `ntimeFieldMappingSuspected = false`, `bitsFieldMappingSuspected = false`, `nonceFieldMappingSuspected = false`, and `refinedHeaderAssemblyReasonCode = "unknown-header-assembly-mismatch"` for all `71` entries with the new fields.
- This final probe did not expose any remaining header-field or packed-header mismatch. In the sampled live path, authoritative and local header80 construction matched exactly, so the remaining divergence is now localized outside coinbase, merkle, and header assembly.
- Snapshot/API alignment remained intact during the final-header step; `/api/health` at `2026-04-17T15:15:50Z` reported `submitAcceptedCount = 70`, `submitRejectedCount = 0`, `submitShareHashValidationCounts.share-hash-invalid = 70`, and `submitTargetValidationCounts.candidate-possible = 70`, and the matching `activity-snapshot.json` at `2026-04-17T15:15:51Z` reported the same counts with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Share-hash target-comparison correction verification completed after restart at `2026-04-17T15:28:37Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-17T15:29:16Z` contained `120` accepted shares.
- Before this corrective patch, fresh live accepted daemon-template shares in equivalent post-restart samples were uniformly `shareHashValidationStatus = "share-hash-invalid"` with `shareHashDiagnostic.reasonCode = "header80-mismatch"`.
- After the corrective patch, the same live path no longer produced an all-invalid result: in the `120`-share post-restart sample, `118` accepted shares reported `shareHashValidationStatus = "share-hash-invalid"` and `2` accepted shares reported `shareHashValidationStatus = "share-hash-valid"`.
- The newest accepted share in that sample reported `shareHashValidationStatus = "share-hash-valid"`, `shareHashValid = true`, and no `shareHashDiagnostic.reasonCode`, which is consistent with the corrected comparison path accepting shares that satisfy the pool share target even when they are not block candidates.
- Snapshot/API alignment remained intact during the corrective step; `/api/health` at `2026-04-17T15:29:05Z` reported `submitAcceptedCount = 118`, `submitRejectedCount = 0`, `submitShareHashValidationCounts = {"preimage-mismatch": 0, "preimage-missing": 0, "share-hash-invalid": 117, "share-hash-valid": 1}`, and `submitTargetValidationCounts.candidate-possible = 118`, and the matching `activity-snapshot.json` at `2026-04-17T15:29:16Z` remained aligned on the same counters with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Dual-threshold operational clarification verification completed after restart at `2026-04-17T15:37:41Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-17T15:38:24Z` contained `94` accepted shares.
- In that post-restart sample, `shareHashValidationStatus` no longer carried the whole operational meaning by itself: `90` accepted shares reported `share-hash-invalid` and `4` reported `share-hash-valid`, while the new dual-threshold booleans were present on the most recent `91` entries after startup.
- Across those `91` entries with the new fields, `88` were `below share target` (`meetsShareTarget = false`), `3` `met the pool share target` (`meetsShareTarget = true`), and `0` `met the block target` (`meetsBlockTarget = true`).
- The newest sampled accepted share reported `shareHashValidationStatus = "share-hash-invalid"`, `meetsShareTarget = false`, `meetsBlockTarget = false`, `shareTargetUsed = "05f5db0a1f000000000000000000000000000000000000000000000000000000"`, `blockTargetUsed = "000000023a7d0000000000000000000000000000000000000000000000000000"`, and `shareHashComparisonMode = "effective-pool-share-target-and-block-target"`.
- This step cleanly separated pool-share validity from block-candidate semantics in the live path: some accepted shares met the pool share target, none of the sampled accepted shares met the block target, and pool-share-valid status no longer implied block-candidate status.
- Snapshot/API alignment remained intact during the dual-threshold clarification step; `/api/health` at `2026-04-17T15:38:10Z` reported `submitAcceptedCount = 88`, `submitRejectedCount = 0`, `submitShareHashValidationCounts = {"preimage-mismatch": 0, "preimage-missing": 0, "share-hash-invalid": 85, "share-hash-valid": 3}`, and `submitTargetValidationCounts.candidate-possible = 88`, and the matching `activity-snapshot.json` at `2026-04-17T15:38:24Z` remained aligned on the same counters with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Candidate-prep verification completed after restart at `2026-04-17T15:49:21Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-17T15:50:02Z` contained `76` accepted shares.
- In that live post-restart sample, none of the accepted shares met the block target: `meetsBlockTarget = false` for all `76`, and `candidatePrepStatus = "candidate-not-triggered"` for all `76`.
- The same live sample therefore confirmed the intended no-send behavior for ordinary shares after the candidate-prep patch: accepted shares continued to flow normally (`share-hash-invalid = 74`, `share-hash-valid = 2` in the `76`-share sample), and no candidate artifact was prepared without `meetsBlockTarget = true`.
- Deterministic unit coverage now proves the triggered path separately: when a daemon-template share is forced to `meetsBlockTarget = true` under a template that includes raw transaction `data`, the candidate-prep hook records `candidatePrepStatus = "candidate-prepared-complete"` and assembles a full candidate block hex locally without calling `submitblock`.
- Snapshot/API alignment remained intact during the candidate-prep step; `/api/health` at `2026-04-17T15:49:53Z` reported `submitAcceptedCount = 73`, `submitRejectedCount = 0`, `submitShareHashValidationCounts = {"preimage-mismatch": 0, "preimage-missing": 0, "share-hash-invalid": 71, "share-hash-valid": 2}`, and `submitTargetValidationCounts.candidate-possible = 73`, and the matching `activity-snapshot.json` at `2026-04-17T15:50:02Z` remained aligned on the same counters with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- No-send `submitblock` dry-run verification completed after restart at `2026-04-18T00:56:40Z`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-18T00:58:13Z` contained `192` accepted shares.
- In that live post-restart sample, none of the accepted shares met the block target: `meetsBlockTarget = false` for all `192`, `candidatePrepStatus = "candidate-not-triggered"` for all `192`, and `submitblockDryRunStatus = "dry-run-not-triggered"` for all `192`.
- The same live sample confirmed the no-send behavior remained unchanged for ordinary shares after the dry-run patch: accepted shares continued to flow normally (`share-hash-invalid = 182`, `share-hash-valid = 10` in the `192`-share sample), and no submitblock dry-run payload was prepared without `meetsBlockTarget = true`.
- Deterministic unit coverage now proves the triggered dry-run path separately: when a daemon-template share is forced to `meetsBlockTarget = true` under a template that includes raw transaction `data`, the dry-run hook records `submitblockDryRunReady = true`, `submitblockDryRunStatus = "dry-run-prepared-complete"`, `submitblockRpcMethod = "submitblock"`, and a complete single-argument payload `[candidateBlockHex]` without calling `submitblock`.
- Snapshot/API alignment remained intact during the dry-run step; `/api/health` at `2026-04-18T00:58:20Z` reported `submitAcceptedCount = 194`, `submitRejectedCount = 0`, `submitShareHashValidationCounts = {"preimage-mismatch": 0, "preimage-missing": 0, "share-hash-invalid": 184, "share-hash-valid": 10}`, and `submitTargetValidationCounts.candidate-possible = 194`, and the matching `activity-snapshot.json` at `2026-04-18T00:58:27Z` remained aligned on the same counters with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Real `submitblock` hook verification completed after restart at `2026-04-18T04:46:12Z` with the new explicit gate still at its default `PEPEPOW_ENABLE_REAL_SUBMITBLOCK=false`; fresh live accepted shares resumed immediately, and a post-restart sample through `2026-04-18T04:46:43Z` contained `68` accepted shares.
- In that fresh live sample, none of the accepted shares met the block target: `meetsBlockTarget = false` for all `68`, `candidatePrepStatus = "candidate-not-triggered"` for all `68`, `submitblockDryRunStatus = "dry-run-not-triggered"` for all `68`, and `submitblockRealSubmitStatus = "submit-not-triggered"` for all `68`.
- The same live sample confirmed there was no accidental daemon-side submission under normal traffic after the real-hook patch: `submitblockAttempted = false` and `submitblockSent = false` for all `68` accepted shares, while accepted share flow remained normal (`share-hash-invalid = 66`, `share-hash-valid = 2`).
- Deterministic unit coverage now proves the explicit gated real-submit path separately: with the flag disabled, no `submitblock` call occurs; with the flag enabled and a complete prepared candidate, exactly one `submitblock(candidateBlockHex)` call occurs; and with the flag enabled but incomplete candidate data, no call occurs and the event records `submitblockRealSubmitStatus = "submit-skipped-incomplete-candidate"`.
- Snapshot/API alignment remained intact during the real-hook step; `/api/health` at `2026-04-18T04:46:25Z` reported `submitAcceptedCount = 90`, `submitRejectedCount = 0`, `submitShareHashValidationCounts = {"preimage-mismatch": 0, "preimage-missing": 0, "share-hash-invalid": 88, "share-hash-valid": 2}`, and `submitTargetValidationCounts.candidate-possible = 90`, and the matching `activity-snapshot.json` at `2026-04-18T04:46:57Z` remained aligned on the same counters with `templateModeEffective = "daemon-template"` and `templateFetchStatus = "ok"`.
- Live-enable readiness guardrail verification completed after the follow-up restart at `2026-04-18T05:11:00Z`; the default-off state remained explicit in startup output (`enable_real_submitblock: false`), and the new operator status fields were visible in `activity-snapshot.json` immediately and in `/api/health` after the API process restart at `2026-04-18T05:12:07Z`.
- In the fresh post-restart live sample through `2026-04-18T05:11:30Z`, accepted shares resumed normally (`83` accepted shares, `share-hash-invalid = 81`, `share-hash-valid = 2`), `meetsBlockTarget = false` for all `83`, and `submitblockRealSubmitStatus = "submit-not-triggered"` with `submitblockAttempted = false` and `submitblockSent = false` for all `83`.
- The new operator-facing status surface remained clean under default-off operation: `/api/health` at `2026-04-18T05:12:15Z` reported `realSubmitblockEnabled = false`, `realSubmitblockAttemptCount = 0`, `realSubmitblockSentCount = 0`, `realSubmitblockErrorCount = 0`, and `realSubmitblockLastStatus = "never-attempted"` while staying aligned with the runtime snapshot on daemon-template reachability and submit counters.
- Live-drill one-shot readiness verification completed after the restart at `2026-04-18T05:41:54Z`; default-off startup output remained explicit (`enable_real_submitblock: false`, `real_submitblock_max_sends: 1`), and the new `./ops/scripts/live-stratum.sh drill-status` command reported `template_mode_effective: daemon-template`, `template_fetch_status: ok`, `template_daemon_rpc_reachable: True`, `real_submit_enabled: False`, `real_submit_send_budget: 1`, `real_submit_send_budget_remaining: 1`, and zero attempt/sent/error counters.
- In the fresh post-restart live sample through `2026-04-18T05:42:23Z`, accepted shares resumed normally (`65` accepted shares, `share-hash-invalid = 61`, `share-hash-valid = 4`), `meetsBlockTarget = false` for all `65`, and the one-shot gate stayed dormant under normal traffic with `submitblockRealSubmitStatus = "submit-not-triggered"`, `submitblockAttempted = false`, and `submitblockSent = false` for all `65`.
- Controlled live `submitblock` drill was executed for one bounded enable window on `2026-04-18`; preflight at `2026-04-18T05:59:26Z` confirmed `templateModeEffective = "daemon-template"`, `templateDaemonRpcReachable = true`, `templateFetchStatus = "ok"`, `realSubmitblockEnabled = false`, `realSubmitblockSendBudget = 1`, and zero attempt/sent/error counters before enablement.
- The drill enable restart at `2026-04-18T05:59:36Z` emitted the expected startup warning for `PEPEPOW_ENABLE_REAL_SUBMITBLOCK=true`; during the observed window from `2026-04-18T05:59:43Z` through `2026-04-18T06:00:59Z`, accepted shares continued flowing (`292` observed by the end of the window) but no share reached `meetsBlockTarget`, so `realSubmitblockAttemptCount = 0`, `realSubmitblockSentCount = 0`, `realSubmitblockErrorCount = 0`, and `realSubmitblockLastStatus = "never-attempted"` throughout.
- Rollback completed with the restart at `2026-04-18T06:01:18Z`; `PEPEPOW_ENABLE_REAL_SUBMITBLOCK` returned to `false`, `realSubmitblockSendBudgetRemaining` returned to `1`, accepted shares resumed immediately after rollback (`278` accepted shares through `2026-04-18T06:02:43Z` with `submitblockRealSubmitStatus = "submit-not-triggered"` for all `278`), and `/api/health` at `2026-04-18T06:02:54Z` remained aligned on `realSubmitblockEnabled = false`, zero attempt/sent/error counters, and the current submit validation counts. Full drill evidence is recorded in `docs/benchmarks/2026-04-18-submitblock-live-drill.md`.
- A controlled low-difficulty isolated testnet drill succeeded on `2026-04-18` without touching the production daemon: an isolated `-testnet` daemon on `127.0.0.1:18884` and a temporary daemon-template stratum ingress on `127.0.0.1:39444` produced a real `meetsBlockTarget = true` share after `51,655` hashes over `6.819s`, recorded `submitblockRealSubmitStatus = "submit-sent"`, and wrote both candidate and follow-up evidence for block hash `000017861966e9a8c0cd19cadad080c01847a16c2ed3a63b3312093cc903df64`.
- The isolated submit evidence confirms the guarded real-submit path works end-to-end under controlled conditions: `candidatePrepStatus = "candidate-prepared-complete"`, `submitblockDryRunStatus = "dry-run-prepared-complete"`, `submitblockAttempted = true`, `submitblockSent = true`, `realSubmitblockSendBudgetRemaining = 0`, and manual follow-up recorded `followupStatus = "match-found"` with observed height `588` on the isolated daemon. Full setup and evidence are recorded in `docs/benchmarks/2026-04-18-isolated-testnet-submitblock-drill.md`.
- Final production baseline recheck completed at `2026-04-18T09:44:58Z`: `pepepow-pool-stratum.service` and `pepepow-pool-api.service` were `active`, only the expected production listeners remained on `127.0.0.1:8834`, `127.0.0.1:8080`, and `0.0.0.0:39333`, no temporary drill listeners remained on `127.0.0.1:18884` or `127.0.0.1:39444`, `/api/health` still reported `templateModeEffective = "daemon-template"`, `templateFetchStatus = "ok"`, `realSubmitblockEnabled = false`, zero attempt/sent/error counters, and live accepted-share flow remained present with `activityLastShareAt = "2026-04-18T09:44:58Z"`.
